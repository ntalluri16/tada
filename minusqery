DECLARE
   v_exists NUMBER;
BEGIN
   SELECT COUNT(*)
   INTO v_exists
   FROM user_tables
   WHERE table_name = 'TADA';

   IF v_exists > 0 THEN
      EXECUTE IMMEDIATE 'DROP TABLE TADA';
   END IF;
END;
/

CREATE TABLE TADA (
   field_name VARCHAR2(50)
);
